---
import Layout from '../layouts/Layout.astro';
import { getTopicPapers, topics } from '../data/research';
---

<Layout
  title="Research - Mohamad Chehade"
  description="Research areas for Mohamad Chehade, including risk-aware machine learning, data-driven reliability, reinforcement learning, LLMs, and power systems applications."
>
  <header class="page-heading research-heading">
    <p class="eyebrow">Research</p>
    <h1>Research</h1>
    <p>
      My research is organized around one question: how can learning systems make decisions under
      uncertainty in a way that is risk-aware and verifiable? The four directions below develop
      complementary pieces of this question — risk-aware learning provides the objective, verification
      and reliability provide the guarantees, LLM decision systems test the methods on
      language-driven control, and power-system applications ground the work in a reliability-critical
      domain. Papers are intentionally cross-tagged across directions, since most of them advance more
      than one.
    </p>
  </header>

  <section class="section topic-overview">
    <div class="section-heading">
      <div>
        <p class="eyebrow">Themes</p>
        <h2>Research directions</h2>
      </div>
      <p>Each direction includes multiple related papers and paper-derived visuals.</p>
    </div>

    <div class="topic-grid">
      {topics.map((topic) => {
        const topicPapers = getTopicPapers(topic.slug);
        return (
          <article class="topic-card">
            <a class="topic-image" href={`/research/${topic.slug}`} aria-label={`Open ${topic.title}`}>
              <img src={topic.image} alt={topic.imageAlt} />
            </a>
            <div class="topic-body">
              <div class="topic-topline">
                <span>{topic.index}</span>
                <span>{topicPapers.length} papers</span>
              </div>
              <h3><a href={`/research/${topic.slug}`}>{topic.title}</a></h3>
              <p>{topic.summary}</p>
              <p class="topic-focus">{topic.focus}</p>
              <div class="mini-paper-list">
                {topicPapers.map((paper) => (
                  <a href={`/publications/${paper.slug}`}>{paper.shortTitle}</a>
                ))}
              </div>
            </div>
          </article>
        );
      })}
    </div>
  </section>
</Layout>

<style>
  .research-heading {
    max-width: 920px;
  }

  .topic-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1.15rem;
  }

  .topic-card {
    display: grid;
    grid-template-columns: minmax(190px, 0.82fr) minmax(0, 1fr);
    gap: 1rem;
    min-height: 17rem;
    padding: 0.85rem;
    background: var(--paper);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    box-shadow: var(--shadow-soft);
  }

  .topic-image {
    display: block;
    min-height: 100%;
    overflow: hidden;
    background: var(--soft);
    border: 1px solid var(--line);
    border-radius: 6px;
  }

  .topic-image img {
    width: 100%;
    height: 100%;
    min-height: 15rem;
    object-fit: cover;
  }

  .topic-body {
    display: grid;
    align-content: start;
    gap: 0.7rem;
    padding: 0.15rem 0.2rem 0.15rem 0;
  }

  .topic-topline {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
  }

  .topic-topline span {
    display: inline-flex;
    align-items: center;
    min-height: 1.55rem;
    color: var(--burnt);
    background: var(--warm);
    border: 1px solid rgba(191, 87, 0, 0.16);
    border-radius: 6px;
    padding: 0.1rem 0.45rem;
    font-size: 0.78rem;
    font-weight: 850;
  }

  .topic-card h3 {
    font-size: 1.15rem;
  }

  .topic-card h3 a {
    color: var(--charcoal);
    text-decoration: none;
  }

  .topic-card h3 a:hover {
    color: var(--burnt);
  }

  .topic-card p {
    color: var(--muted);
    margin: 0;
  }

  .topic-focus {
    font-size: 0.95rem;
    color: var(--charcoal);
    border-left: 2px solid var(--accent-line);
    padding-left: 0.7rem;
    margin-top: 0.1rem;
    margin-bottom: 0.1rem;
  }

  .mini-paper-list {
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem;
    margin-top: 0.3rem;
  }

  .mini-paper-list a {
    display: inline-flex;
    align-items: center;
    min-height: 1.9rem;
    color: var(--blue);
    background: var(--wash);
    border: 1px solid rgba(10, 103, 133, 0.12);
    border-radius: 6px;
    padding: 0.18rem 0.55rem;
    font-size: 0.9rem;
    font-weight: 700;
    text-decoration: none;
  }

  .mini-paper-list a:hover {
    color: #ffffff;
    background: var(--burnt);
  }

  @media (max-width: 960px) {
    .topic-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    .topic-card {
      grid-template-columns: 1fr;
    }

    .topic-image img {
      min-height: 12rem;
    }
  }
</style>
