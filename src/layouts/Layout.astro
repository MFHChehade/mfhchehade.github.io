---
interface Props {
  title: string;
  description?: string;
}

const {
  title,
  description = "Mohamad Chehade is a Ph.D. student at The University of Texas at Austin working on risk-aware machine learning, data-driven reliability, reinforcement learning, LLMs, and power systems applications.",
} = Astro.props;

const currentPath = Astro.url.pathname.replace(/\/$/, "") || "/";
const navLinks = [
  { href: "/", label: "Home" },
  { href: "/research", label: "Research" },
  { href: "/publications", label: "Publications" },
  { href: "/contact", label: "Contact" },
];

const isActive = (href: string) => href === "/" ? currentPath === "/" : currentPath.startsWith(href);
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:image" content="/profile.jpeg" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
  </head>
  <body>
    <a class="skip-link" href="#content">Skip to content</a>
    <header class="site-header">
      <div class="site-header-inner">
        <a href="/" class="site-name" aria-label="Mohamad Chehade home">
          <span class="site-mark">MC</span>
          <span>Mohamad Chehade</span>
        </a>
        <nav class="site-nav" aria-label="Main navigation">
          {navLinks.map((link) => (
            <a
              href={link.href}
              class:list={[{ active: isActive(link.href) }]}
              aria-current={isActive(link.href) ? "page" : undefined}
            >
              {link.label}
            </a>
          ))}
          <a href="/CV_Mohamad.pdf" target="_blank" rel="noopener noreferrer">CV</a>
        </nav>
      </div>
    </header>

    <main id="content">
      <slot />
    </main>

    <footer>
      <div class="footer-inner">
        <p>&copy; {new Date().getFullYear()} Mohamad Chehade</p>
        <p>Reliable learning systems for risk-aware decisions.</p>
      </div>
    </footer>
  </body>
</html>

<style is:global>
  :root {
    --burnt: #bf5700;
    --burnt-dark: #8d3f00;
    --charcoal: #263341;
    --ink: #15212d;
    --muted: #536273;
    --blue: #0a6785;
    --green: #24705f;
    --bg: #fbfaf7;
    --paper: #ffffff;
    --wash: #ecf4f4;
    --warm: #fff5eb;
    --soft: #f2f5f3;
    --line: #d8dedc;
    --line-strong: #bdc7c5;
    --shadow: 0 26px 70px rgba(23, 32, 42, 0.1);
    --shadow-soft: 0 14px 36px rgba(23, 32, 42, 0.07);
    --radius: 8px;
    --max-width: 1180px;
  }

  * {
    box-sizing: border-box;
  }

  html {
    color: var(--ink);
    background: var(--bg);
    font-family:
      Aptos,
      "Segoe UI",
      ui-sans-serif,
      system-ui,
      -apple-system,
      BlinkMacSystemFont,
      sans-serif;
    font-size: 16px;
    scroll-behavior: smooth;
  }

  body {
    min-height: 100vh;
    margin: 0;
    line-height: 1.6;
    display: flex;
    flex-direction: column;
    background: var(--bg);
    border-top: 5px solid var(--burnt);
  }

  img {
    max-width: 100%;
    display: block;
  }

  main {
    width: min(100% - 2rem, var(--max-width));
    flex: 1;
    margin: 0 auto;
    padding: 0 0 4.5rem;
  }

  .skip-link {
    position: fixed;
    top: 0.75rem;
    left: 0.75rem;
    z-index: 100;
    padding: 0.45rem 0.65rem;
    color: #ffffff;
    background: var(--charcoal);
    border-radius: 4px;
    transform: translateY(-150%);
  }

  .skip-link:focus {
    transform: translateY(0);
  }

  .site-header {
    position: sticky;
    top: 0;
    z-index: 20;
    background: rgba(247, 248, 243, 0.9);
    border-bottom: 1px solid rgba(217, 222, 227, 0.86);
    backdrop-filter: blur(18px);
  }

  .site-header-inner {
    width: min(100% - 2rem, var(--max-width));
    min-height: 76px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
  }

  .site-name {
    min-width: 0;
    display: inline-flex;
    align-items: center;
    gap: 0.65rem;
    color: var(--ink);
    font-weight: 850;
    text-decoration: none;
    white-space: nowrap;
  }

  .site-mark {
    width: 2.35rem;
    height: 2.35rem;
    display: inline-grid;
    place-items: center;
    color: #ffffff;
    background: var(--burnt);
    border-radius: 6px;
    font-size: 0.76rem;
    font-weight: 850;
  }

  .site-name:hover {
    color: var(--burnt);
    text-decoration: none;
  }

  .site-nav {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: 0.25rem;
    font-size: 0.95rem;
  }

  .site-nav a {
    min-height: 2.4rem;
    display: inline-flex;
    align-items: center;
    color: var(--charcoal);
    text-decoration: none;
    border-bottom: 2px solid transparent;
    padding: 0.35rem 0.72rem;
    transition:
      background 160ms ease,
      border-color 160ms ease,
      color 160ms ease;
  }

  .site-nav a:hover,
  .site-nav a.active {
    color: var(--burnt);
    background: rgba(255, 255, 255, 0.58);
    border-color: var(--burnt);
  }

  footer {
    color: var(--muted);
    font-size: 0.9rem;
    padding: 1.4rem 1rem;
    background: #ffffff;
    border-top: 1px solid var(--line);
  }

  .footer-inner {
    width: min(100%, var(--max-width));
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 0.8rem;
  }

  h1,
  h2,
  h3 {
    color: var(--charcoal);
    margin: 0;
    line-height: 1.16;
  }

  h1,
  h2 {
    font-family: Georgia, Cambria, "Times New Roman", ui-serif, serif;
  }

  h1 {
    font-size: 3.45rem;
    font-weight: 700;
  }

  h2 {
    font-size: 1.55rem;
    font-weight: 700;
    margin-bottom: 1rem;
  }

  h3 {
    font-size: 1.03rem;
    font-weight: 750;
  }

  p {
    margin: 0 0 0.85rem;
  }

  a {
    color: var(--blue);
    text-underline-offset: 0.18em;
    transition: color 160ms ease;
  }

  a:hover {
    color: var(--burnt);
  }

  ul,
  ol {
    margin: 0.35rem 0 0.75rem 1.2rem;
    padding: 0;
  }

  li {
    margin: 0.3rem 0;
  }

  .plain-list {
    list-style: none;
    margin-left: 0;
  }

  :focus-visible {
    outline: 2px solid var(--burnt);
    outline-offset: 3px;
  }

  .page-heading {
    max-width: 860px;
    padding: 3.2rem 0 2rem;
    margin-bottom: 0.75rem;
  }

  .page-heading p,
  .lead {
    color: var(--muted);
    font-size: 1.1rem;
  }

  .eyebrow {
    color: var(--burnt);
    font-size: 0.74rem;
    font-weight: 850;
    text-transform: uppercase;
    margin-bottom: 0.45rem;
  }

  .section {
    margin-top: 3rem;
  }

  .section:first-child {
    margin-top: 0;
  }

  .section-heading {
    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 1rem;
  }

  .section-heading h2 {
    margin-bottom: 0;
  }

  .section-heading p {
    color: var(--muted);
    margin: 0;
    max-width: 520px;
  }

  .button-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.7rem;
  }

  .button {
    min-height: 2.5rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    color: #ffffff;
    background: var(--burnt);
    border: 1px solid var(--burnt);
    border-radius: 6px;
    padding: 0.5rem 0.82rem;
    font-weight: 750;
    text-decoration: none;
    transition:
      background 160ms ease,
      border-color 160ms ease,
      color 160ms ease,
      transform 160ms ease;
  }

  .button:hover {
    color: #ffffff;
    background: var(--burnt-dark);
    border-color: var(--burnt-dark);
    text-decoration: none;
    transform: translateY(-1px);
  }

  .button.secondary {
    color: var(--charcoal);
    background: transparent;
    border-color: var(--line);
  }

  .button.secondary:hover {
    color: var(--burnt);
    background: var(--warm);
    border-color: #efc7a5;
  }

  .modern-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 1.05rem;
  }

  .surface {
    background: var(--paper);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    box-shadow: var(--shadow-soft);
  }

  .publication-list,
  .news-list {
    display: grid;
    gap: 0;
  }

  .publication-entry {
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    padding: 1.15rem 0;
    background: transparent;
    border-top: 1px solid var(--line);
    transition:
      transform 160ms ease,
      border-color 160ms ease;
  }

  .publication-entry:last-child {
    border-bottom: 1px solid var(--line);
  }

  .publication-entry:hover {
    border-color: var(--line-strong);
    transform: translateY(-1px);
  }

  .publication-entry h3 {
    color: var(--ink);
    margin-bottom: 0.35rem;
  }

  .publication-entry h3 a {
    color: var(--ink);
    text-decoration: none;
  }

  .publication-entry h3 a:hover {
    color: var(--burnt);
  }

  .pub-topline {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
    margin-bottom: 0.45rem;
  }

  .pub-topline span {
    display: inline-flex;
    align-items: center;
    min-height: 1.6rem;
    color: var(--burnt);
    background: var(--warm);
    border: 1px solid rgba(191, 87, 0, 0.16);
    border-radius: 6px;
    padding: 0.1rem 0.45rem;
    font-size: 0.78rem;
    font-weight: 850;
  }

  .publication-entry p {
    margin-bottom: 0.28rem;
  }

  .authors {
    color: var(--ink);
  }

  .venue,
  .meta {
    color: var(--muted);
    font-style: italic;
  }

  .link-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    font-size: 0.92rem;
    margin-top: 0.55rem;
  }

  .link-row a {
    display: inline-flex;
    align-items: center;
    min-height: 1.85rem;
    color: var(--blue);
    background: var(--wash);
    border: 1px solid rgba(10, 103, 133, 0.12);
    border-radius: 6px;
    padding: 0.2rem 0.58rem;
    text-decoration: none;
  }

  .link-row a:hover {
    color: #ffffff;
    background: var(--burnt);
  }

  @media (max-width: 820px) {
    main,
    .site-header-inner {
      width: min(100% - 1.2rem, var(--max-width));
    }

    .site-header {
      position: static;
    }

    .site-header-inner {
      align-items: stretch;
      flex-direction: column;
      gap: 0.75rem;
      padding: 0.9rem 0;
    }

    .site-nav {
      justify-content: flex-start;
      flex-wrap: nowrap;
      overflow-x: auto;
      scrollbar-width: none;
    }

    .site-nav::-webkit-scrollbar {
      display: none;
    }

    .site-nav a {
      flex: 0 0 auto;
    }

    h1 {
      font-size: 2.45rem;
    }

    h2 {
      font-size: 1.42rem;
    }

    .modern-grid {
      grid-template-columns: 1fr;
    }

    .section-heading {
      align-items: start;
      flex-direction: column;
    }
  }

  @media (max-width: 520px) {
    main,
    .site-header-inner {
      width: min(100% - 1rem, var(--max-width));
    }

    h1 {
      font-size: 2.05rem;
    }

    .site-nav {
      gap: 0.1rem;
      font-size: 0.88rem;
    }

    .site-nav a {
      padding: 0.32rem 0.5rem;
    }

    .button {
      width: 100%;
    }
  }
</style>
